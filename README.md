# AIXCraft-Builder

Interface web moderna e escalável para dashboards de produto.

## Executar localmente

Como o projeto é estático, basta abrir o arquivo `index.html` no navegador.

Opcionalmente, você pode usar um servidor local:

```bash
python3 -m http.server 8080
```

E acessar `http://localhost:8080`.
